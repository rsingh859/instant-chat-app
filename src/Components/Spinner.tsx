import React from "react";

type Props = {};

const Spinner = ({}: Props) => {
  return (
    <div className="border-2 animate-spin border-t-myBlue w-5 h-5 rounded-full " />
  );
};

export default Spinner;
